name: 'Install Qt'
description: 'Installs Qt using a centralized version for all vclib workflows'
runs:
  using: 'composite'
  steps:
    - name: Install Qt
      uses: jurplel/install-qt-action@v4
      with:
        cache: 'true'
        version: '6.10.*'
