- Use ModelMatrix and ViewMatrix in cs_drawable_selection to calculate a proper selection (since the selection box lives on the view plane)
- Face selection
- Edge selection
- Selection modes (add, subtract, etc...)
- Figure out a proper implementation location in the c++ code