ENUM_PREFIX DRAWABLE_MESH_SURFACE_ID
DEFINE_PREFIX SURFACE_ID

# VS is static (not generated for each combination)
VS_FILE vs_surface_id.sc

# FS is generated dynamically based on combinations
FS_IN fs_surface_id_in.sh
FS_PREFIX fs_surface_id

DIM_FS IS_TRI_MESH
    ON
    OFF
